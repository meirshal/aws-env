{
  "name": "aws-env",
  "version": "1.0.0",
  "description": "A tool for injecting secrets and non-secret configuration, managed on AWS, as environment variables",
  "scripts": ["aws-env.sh"],
  "global": "true",
  "install": "install -b aws-env.sh ${PREFIX:-/usr/local}/bin/aws-env"
}
